Note: ui-spec-latest.tar.gz is a tarball of an Apple Pages bundle, which I've
temporarily switched to using due to an annoying bug in OpenOffice.  I
intend to switch back to .odt format as soon as it's feasible to do so...
